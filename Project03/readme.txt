To compile please use the given makefile.
Simply type make in the console window and
an executable file should be created with 
the name "buffers".

You can also manually compile using
gcc buffers.c -o buffers -lpthread -lrt
An executable file named "buffers" will
be created.